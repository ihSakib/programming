#include <bits/stdc++.h>
using namespace std;

int main()
{
  // string myName, friendName;

  // cout << "Enter your name: ";
  // getline(cin, myName);

  // cout << "Enter your friend's name: ";
  // getline(cin, friendName);

  // cout << "Your name is " << myName << " and friend's name is " << friendName << endl;

  cout << acos(0) * (180 / M_PI)<<endl;
  cout<< ceil(1.1);
  
  return 0;
}